schema_version: 1
recipe:
  name: boost
  upstream_version: "1.86.0"
  cvc_revision: 1
  maintainer: "CyberPC Angel, LLC"
  maintainer_email: "joe@angelx.dev"
  description: "Boost C++ Libraries — portable, peer-reviewed C++ source libraries for general-purpose programming."
  homepage: https://www.boost.org/
  license: BSL-1.0
  tags: [utils]

source:
  type: tarball
  url: https://github.com/boostorg/boost/releases/download/boost-1.86.0/boost-1.86.0-cmake.tar.gz
  sha256: "c62ce6e64d34414864fef946363db91cea89c1b90360eabed0515f0eda74c75c"
  strip_components: 1

patches: []

depends:
  build:
    - name: zlib
      version: "^1.3"
  host_tools:
    - cmake
    - ninja

build:
  matrix:
    - platform: linux
      script: build.sh
      env:
        CXXFLAGS: "-O2 -fPIC -std=c++17"
    - platform: macos
      script: build.sh
      env:
        MACOSX_DEPLOYMENT_TARGET: "13.0"
    - platform: windows
      script: build.ps1
      vcpkg_port: boost-thread

package:
  files:
    - lib/libboost*
    - lib/boost*
    - lib/cmake/Boost*/
    - lib/cmake/boost*/
    - include/boost/
  cmake_packages:
    - { name: Boost, targets: ["Boost::boost"] }

abi:
  cxx_std: 17
