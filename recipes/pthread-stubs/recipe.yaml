schema_version: 1
recipe:
  name: pthread-stubs
  upstream_version: "0.5"
  cvc_revision: 1
  maintainer: "cvcpkg group"
  maintainer_email: "info@cvcpkg.org"
  maintainer_url: "https://cvcpkg.org"
  homepage: https://www.x.org/
  license: MIT
  tags: [libraries, x11, graphics]
  description: >-
    libpthread-stubs — weak-symbol stubs for the pthread functions, so a
    library can reference them without hard-linking libpthread on platforms
    where they live elsewhere (or in libc). libxcb's configure requires the
    pthread-stubs.pc; it is system-provided on Linux/FreeBSD/NetBSD but absent
    on OpenBSD, so package it for a hermetic + OpenBSD-complete libxcb.

source:
  type: tarball
  url: https://xorg.freedesktop.org/releases/individual/lib/libpthread-stubs-0.5.tar.xz
  sha256: "59da566decceba7c2a7970a4a03b48d9905f1262ff94410a649224e33d2442bc"
  strip_components: 1

patches: []

depends:
  build:
    - pkg-config
  runtime: []
  host_tools: []

package:
  files:
    - lib/
    - include/
    - share/

build:
  matrix:
    - platform: linux
      script: build.sh
    - platform: freebsd
      script: build.sh
    - platform: openbsd
      script: build.sh
    - platform: netbsd
      script: build.sh
