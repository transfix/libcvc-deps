schema_version: 1
recipe:
  name: contourpy-cp313
  upstream_version: "1.3.1"
  # Bumped 3 -> 4: hand-converted — every server build of this recipe
  # died in meson with 'Dependency "pybind11" not found' (pkg-config,
  # config-tool and cmake all miss the staged copy).  Reproduced locally,
  # fixed via pybind11's own --pkgconfigdir, verified green.
  cvc_revision: 4

  maintainer: "cvcpkg group"
  maintainer_email: "info@cvcpkg.org"
  maintainer_url: "https://cvcpkg.org"
  homepage: https://pypi.org/project/contourpy/
  license: "BSD-3-Clause"
  tags: [python, source]
  description: >-
    contourpy 1.3.1 for CPython 3.13 — the cp313 column of
    cvcpkg's per-interpreter matrix (C extensions compiled for cp313 from the PyPI sdist).
    HAND-CONVERTED from the generated shape: meson must find the staged
    pybind11 through its published .pc dir (the shipped pybind11-config
    script has a dead ephemeral-prefix shebang), which the generator
    cannot infer — see build.sh step 2b.
    Built FROM SOURCE: cvcpkg fetches and sha256-verifies the PyPI sdist,
    and build.sh compiles the wheel with the prefix's own CPython 3.13 —
    no third-party prebuilt binary enters the bundle.

# From-source sdist, sha256 as published by PyPI for this exact version.
source:
  type: tarball
  url: https://files.pythonhosted.org/packages/25/c2/fc7193cc5383637ff390a712e88e4ded0452c9fbcf84abe3de5ea3df1866/contourpy-1.3.1.tar.gz
  sha256: "dfd97abd83335045a913e3bcc4a09c0ceadbe66580cf573fe961f4a825efa699"
  strip_components: 1

python:
  interpreter: python313
  abi: cp313

patches: []

depends:
  build:
    - name: python313
    - name: numpy-cp313
    # PEP-517 build backend, from this sdist's [build-system] requires.
    # --no-build-isolation means pip will NOT fetch it, so it has to be
    # in the build prefix already (build.sh bridges it onto sys.path).
    - name: meson
    - name: meson-python-cp313
    - name: pybind11-cp313
  runtime:
    - name: python313
    - name: numpy-cp313
  host_tools:
    - python313

build:
  build_type_independent: true
  matrix:
    - platform: linux
      script: build.sh
    - platform: macos
      script: build.sh
    - platform: windows
      script: build.ps1

package:
  files:
    - lib/python3.13/site-packages/contourpy/
    - lib/python3.13/site-packages/contourpy-*.dist-info/
    - Lib/site-packages/contourpy/
    - Lib/site-packages/*.dist-info/
