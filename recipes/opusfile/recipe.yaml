schema_version: 1
recipe:
  name: opusfile
  upstream_version: "0.12"
  cvc_revision: 1
  maintainer: "cvcpkg group"
  maintainer_email: "info@cvcpkg.org"
  maintainer_url: "https://cvcpkg.org"
  homepage: https://opus-codec.org/
  license: BSD-3-Clause
  tags: [libraries, audio, multimedia]
  description: >-
    opusfile — a high-level API for decoding and seeking within .opus
    files (Ogg-encapsulated Opus streams).  Used by the allegro5 audio
    addon (acodec).  Built with --disable-http to avoid an OpenSSL
    dependency.

source:
  type: tarball
  url: https://downloads.xiph.org/releases/opus/opusfile-0.12.tar.gz
  sha256: "118d8601c12dd6a44f52423e68ca9083cc9f2bfe72da7a8c1acb22a80ae3550b"
  strip_components: 1

patches: []

depends:
  build:
    - pkg-config
  runtime:
    - name: libopus
    - name: libogg
  host_tools: []

build:
  matrix:
    - platform: linux
      script: build.sh
      env:
        CFLAGS: "-O2 -fPIC"
    - platform: macos
      script: build.sh
      env:
        MACOSX_DEPLOYMENT_TARGET: "13.0"
    - platform: freebsd
      script: build.sh
      env:
        CFLAGS: "-O2 -fPIC"
    - platform: openbsd
      script: build.sh
      env:
        CFLAGS: "-O2 -fPIC"
    - platform: netbsd
      script: build.sh
      env:
        CFLAGS: "-O2 -fPIC"

package:
  files:
    - lib/libopusfile*
    - lib/pkgconfig/opusfile.pc
    - include/opus/
  pkg_config:
    - opusfile
