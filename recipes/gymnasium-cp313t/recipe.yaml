schema_version: 1
recipe:
  name: gymnasium-cp313t
  upstream_version: "1.3.0"
  cvc_revision: 1

  maintainer: "cvcpkg group"
  maintainer_email: "info@cvcpkg.org"
  maintainer_url: "https://cvcpkg.org"
  homepage: https://pypi.org/project/gymnasium/
  license: "MIT"
  tags: [python, source]
  description: >-
    gymnasium 1.3.0 for CPython 3.13t — the cp313t column of
    cvcpkg's per-interpreter matrix (pure-Python, built from the PyPI sdist);
    generated by tools/gen_python_recipes.py.
    Built FROM SOURCE: cvcpkg fetches and sha256-verifies the PyPI sdist,
    and build.sh compiles the wheel with the prefix's own CPython 3.13t —
    no third-party prebuilt binary enters the bundle.
    The cp313t column installs under the free-threaded (no-GIL)
    interpreter and its import check runs with the GIL disabled.

# From-source sdist, sha256 as published by PyPI for this exact version.
source:
  type: tarball
  url: https://files.pythonhosted.org/packages/4d/ff/14b6880d703dfaca204490979d3254ccd280c99550798993319902873658/gymnasium-1.3.0.tar.gz
  sha256: "6939e86e835d6b71b6ba6bfd360487420876deafc79bfb7bacba83a7c446bcf3"
  strip_components: 1

python:
  interpreter: python313t
  abi: cp313t

patches: []

depends:
  build:
    - name: python313t
    - name: cloudpickle-cp313t
    - name: farama-notifications-cp313t
    - name: numpy-cp313t
    - name: typing-extensions-cp313t
    # PEP-517 build backend, from this sdist's [build-system] requires.
    # --no-build-isolation means pip will NOT fetch it, so it has to be
    # in the build prefix already (build.sh bridges it onto sys.path).
    - name: setuptools-cp313t
  runtime:
    - name: python313t
    - name: cloudpickle-cp313t
    - name: farama-notifications-cp313t
    - name: numpy-cp313t
    - name: typing-extensions-cp313t
  host_tools:
    - python313t

build:
  build_type_independent: true
  matrix:
    - platform: any
      script: build.sh
    - platform: windows
      script: build.ps1

package:
  files:
    - lib/python3.13t/site-packages/gymnasium/
    - lib/python3.13t/site-packages/gymnasium-*.dist-info/
    - Lib/site-packages/gymnasium/
    - Lib/site-packages/*.dist-info/
