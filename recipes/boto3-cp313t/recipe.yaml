schema_version: 1
recipe:
  name: boto3-cp313t
  upstream_version: "1.43.49"
  cvc_revision: 1

  maintainer: "cvcpkg group"
  maintainer_email: "info@cvcpkg.org"
  maintainer_url: "https://cvcpkg.org"
  homepage: https://pypi.org/project/boto3/
  license: "Apache-2.0"
  tags: [python, wheel]
  description: >-
    boto3 1.43.49 for CPython 3.13t — the cp313t column of
    cvcpkg's per-interpreter wheel matrix (pure-Python wheel, same artifact in every column);
    generated by tools/gen_python_recipes.py.
    The cp313t column installs under the free-threaded (no-GIL)
    interpreter and its import check runs with the GIL disabled.

source:
  type: python_wheel
  artifacts:
    any:
      url: https://files.pythonhosted.org/packages/a3/08/73efb432f9da880deb91eded8ed41d23f03ec7ac47af9b8ecf884f276876/boto3-1.43.49-py3-none-any.whl
      sha256: "2b31ab1a0eb1cee01d0363b8ee5e68b45dff2c8f99e7b53aca11c9f7b591a794"

python:
  interpreter: python313t
  abi: cp313t

patches: []

depends:
  build:
    - name: python313t
    - name: botocore-cp313t
    - name: jmespath-cp313t
    - name: s3transfer-cp313t
  runtime:
    - name: python313t
    - name: botocore-cp313t
    - name: jmespath-cp313t
    - name: s3transfer-cp313t

build:
  build_type_independent: true
  matrix:
    - platform: any
      script: build.sh

package:
  files:
    - lib/python3.13t/site-packages/boto3/
    - lib/python3.13t/site-packages/*.dist-info/
