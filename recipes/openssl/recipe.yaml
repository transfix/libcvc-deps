schema_version: 1
recipe:
  name: openssl
  upstream_version: "3.4.1"
  cvc_revision: 2
  maintainer: "cvcpkg group"
  maintainer_email: "info@cvcpkg.org"
  maintainer_url: "https://cvcpkg.org"
  description: "OpenSSL — a robust, full-featured toolkit for TLS/SSL and general-purpose cryptography."
  homepage: https://www.openssl.org/
  license: Apache-2.0
  tags: [networking, crypto, wasm]

source:
  type: tarball
  url: https://github.com/openssl/openssl/releases/download/openssl-3.4.1/openssl-3.4.1.tar.gz
  sha256: "002a2d6b30b58bf4bea46c43bdd96365aaf8daa6c428782aa4feee06da197df3"
  strip_components: 1

patches: []

depends:
  build: []
  host_tools:
    - perl
    - nasm

build:
  matrix:
    - platform: linux
      script: build.sh
      env:
        CFLAGS: "-O2 -fPIC"
    - platform: macos
      script: build.sh
      env:
        MACOSX_DEPLOYMENT_TARGET: "13.0"
    - platform: freebsd
      script: build.sh
      env:
        CFLAGS: "-O2 -fPIC"
    - platform: openbsd
      script: build.sh
      env:
        CFLAGS: "-O2 -fPIC"
    - platform: netbsd
      script: build.sh
      env:
        CFLAGS: "-O2 -fPIC"
    - platform: windows
      script: build.ps1
    - platform: wasm
      host_platform: linux
      script: build-wasm.sh
    - platform: wasm
      host_platform: windows
      script: build-wasm.ps1
    - platform: wasi
      host_platform: linux
      script: build-wasi.sh
    - platform: wasi
      host_platform: windows
      script: build-wasi.ps1

package:
  files:
    - lib/libssl*
    - lib/libcrypto*
    - lib/ssl*
    - lib/crypto*
    - lib/cmake/OpenSSL/
    - lib/pkgconfig/openssl*
    - lib/pkgconfig/libssl*
    - lib/pkgconfig/libcrypto*
    - include/openssl/
    - bin/openssl*
  cmake_packages:
    - { name: OpenSSL, targets: ["OpenSSL::SSL", "OpenSSL::Crypto"] }

abi:
  cxx_std: 17
