# Build and publish all macOS recipes to cvcpkg.org.
#
# Since macOS has no persistent builder service (GitHub-hosted runners
# are ephemeral), this workflow builds the full recipe DAG locally in
# parallel shards and publishes each package directly to the server.
#
# Triggered manually via workflow_dispatch, or by the populate-server
# workflow when 'macos' is in the platforms list.
#
# Required repo secrets:
#   CVCPKG_PUBLISHER_TOKEN  — a publisher-role token for cvcpkg.org

name: macOS Package Build

on:
  workflow_dispatch:
    inputs:
      config:
        description: "Build config"
        required: true
        default: "release"
        type: choice
        options:
          - release
          - debug
      link:
        description: "Link mode"
        required: true
        default: "shared"
        type: choice
        options:
          - shared
          - static
      server:
        description: "cvcpkg server URL"
        required: true
        default: "https://cvcpkg.org"
        type: string
      shards:
        description: "Number of parallel shards"
        required: true
        default: "4"
        type: choice
        options:
          - "2"
          - "3"
          - "4"
          - "5"
          - "6"
      ref:
        description: "Git ref to build recipes from (default: prod)"
        required: false
        default: "prod"
        type: string
  workflow_call:
    inputs:
      config:
        required: true
        type: string
      link:
        required: true
        type: string
      server:
        required: true
        type: string
      shards:
        required: false
        type: string
        default: "4"
      ref:
        required: false
        type: string
        default: "prod"

permissions:
  contents: read

jobs:
  # Generate the shard matrix dynamically based on inputs.shards
  setup:
    name: Setup matrix
    runs-on: ubuntu-latest
    outputs:
      shards: ${{ steps.gen.outputs.shards }}
    steps:
      - id: gen
        run: |
          TOTAL="${{ inputs.shards || '4' }}"
          # Generate JSON array [0, 1, ..., TOTAL-1]
          SHARDS=$(python3 -c "import json; print(json.dumps(list(range(int('$TOTAL')))))")
          echo "shards=$SHARDS" >> "$GITHUB_OUTPUT"

  build-shard:
    name: macOS shard ${{ matrix.shard }}/${{ inputs.shards || '4' }} (${{ inputs.config }}/${{ inputs.link }})
    needs: setup
    runs-on: macos-latest
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJSON(needs.setup.outputs.shards) }}

    steps:
      - uses: actions/checkout@v5
        with:
          # Defaults to prod (production recipe set); override to validate a
          # branch/master on macOS before promoting it to prod.
          ref: ${{ inputs.ref || 'prod' }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install system build tools
        run: |
          brew install ninja autoconf automake libtool pkg-config libomp meson flex bison
          brew install llvm@18 2>/dev/null || true
          # Pin cmake to 3.x — Boost 1.86 cmake files are incompatible with cmake 4.x
          pip3 install --break-system-packages "cmake>=3.31,<4"

      - name: Install cvcpkg
        run: |
          pip3 install --break-system-packages .
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          echo "$(python3 -m site --user-base)/bin" >> "$GITHUB_PATH"

      - name: Build shard ${{ matrix.shard }} (pack-all)
        env:
          CVCPKG_SERVER_URL: ${{ inputs.server }}
          CVCPKG_TOKEN: ${{ secrets.CVCPKG_PUBLISHER_TOKEN }}
        run: |
          set -euo pipefail
          SHARDS="${{ inputs.shards || '4' }}"
          mkdir -p "${{ github.workspace }}/prefix"
          mkdir -p "${{ github.workspace }}/dist"

          cvcpkg pack-all \
            --keep-going \
            --platform macos \
            --config "${{ inputs.config }}" \
            --link "${{ inputs.link }}" \
            --prefix "${{ github.workspace }}/prefix" \
            --output-dir "${{ github.workspace }}/dist" \
            --recipes-dir recipes \
            --shard "${{ matrix.shard }}/${SHARDS}" \
            --server-cache "$CVCPKG_SERVER_URL" \
            --server-cache-token "$CVCPKG_TOKEN" \
            --server-cache-push

      - name: List built packages
        if: always()
        run: |
          echo "Built packages (shard ${{ matrix.shard }}):"
          ls -lh "${{ github.workspace }}/dist/" 2>/dev/null || echo "(no packages)"
          echo ""
          COUNT=$(ls "${{ github.workspace }}/dist/"*.tar.zst 2>/dev/null | wc -l || echo 0)
          echo "Count: $COUNT archives"

      - name: Publish to cvcpkg.org
        if: always()
        env:
          CVCPKG_SERVER_URL: ${{ inputs.server }}
          CVCPKG_TOKEN: ${{ secrets.CVCPKG_PUBLISHER_TOKEN }}
        run: |
          set -euo pipefail
          DIST="${{ github.workspace }}/dist"

          # Count archives
          ARCHIVES=($(ls "$DIST"/*.tar.zst 2>/dev/null || true))
          if [ ${#ARCHIVES[@]} -eq 0 ]; then
            echo "::warning::No archives found in dist/ — nothing to publish"
            exit 0
          fi

          echo "Publishing ${#ARCHIVES[@]} packages to $CVCPKG_SERVER_URL"
          cvcpkg publish --all \
            --server "$CVCPKG_SERVER_URL" \
            --token "$CVCPKG_TOKEN" \
            --output-dir "$DIST" \
            --platform macos \
            --config "${{ inputs.config }}" \
            --link "${{ inputs.link }}"

      - name: Upload dist artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: macos-packages-shard${{ matrix.shard }}-${{ inputs.config }}-${{ inputs.link }}
          path: dist/
          if-no-files-found: warn
          retention-days: 7
