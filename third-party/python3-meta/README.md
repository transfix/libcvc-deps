# python3 meta-package

This directory is the (empty) vendored source tree for the `python3`
meta-recipe. The recipe builds no upstream source; its build script only
creates the generic `python3`/`python`/`pip3`/`pip` aliases that point at the
default CPython (python313). See recipes/python3/.
