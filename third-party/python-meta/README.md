# python-meta

Placeholder source tree for the `python` meta-recipe. The recipe ships no
upstream source: its build.sh creates the generic `python` / `pip` symlinks
pointing at the default CPython (via the `python3` meta + python313).
